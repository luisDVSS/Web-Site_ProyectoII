import { Component } from "react";
import AdminBuilder from "./FuncMaker";
export default {
  title: "Auth / AdminBuilder",
  component: AdminBuilder,
};
export const Default = {
  args: {
    loading: false,
    error: "",
  },
};
